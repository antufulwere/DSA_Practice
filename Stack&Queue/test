// Online C++ compiler to run C++ program online
#include <iostream>
#include <vector>
using namespace std;
class LRUCache {
public:
    vector<pair<int,int>> vec;
    int cap;
    int LRU;
    LRUCache(int capacity) {
            vec.resize(capacity);
            cap=capacity;
    }
    
    int get(int key) {
     for(auto v : vec){
         if(v.first==key){
            return v.second;
         }
     }   
     return -1;
    }
    
    void put(int key, int value) {
        // check vector size is full 
        if(vec.size()==cap){
            
        }else{
            // check if key already exist update value and return
            for(auto v : vec){
                 if(v.first==key){
                    v.second=value;
                    return;
                 }
            }
            // if not push new key value in vector 
            vec.push_back({key,value});
            LRU=key;
        } 
    }
};
int main() {
    LRUCache* obj = new LRUCache(2);
    cout<<obj->get(1);
    
    return 0;
}